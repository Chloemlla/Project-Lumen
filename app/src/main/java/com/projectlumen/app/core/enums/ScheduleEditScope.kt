package com.projectlumen.app.core.enums

/** Scope chosen by the user when editing or deleting one occurrence of a repeating series. */
enum class ScheduleEditScope {
    THIS_ONLY,
    THIS_AND_FUTURE,
    ALL,
}
