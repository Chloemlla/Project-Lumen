<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:name="com.project.lumen.permission.ACCESS_LUMEN_CORE"
        android:description="@string/permission_access_lumen_core_description"
        android:label="@string/permission_access_lumen_core_label"
        android:protectionLevel="dangerous" />
    <permission
        android:name="com.project.lumen.permission.TRIGGER_LUMEN_CONTROL"
        android:description="@string/permission_trigger_lumen_control_description"
        android:label="@string/permission_trigger_lumen_control_label"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- The deadline alert has to buzz even with the device in the user's pocket, which is the whole
         point of the "forced" node in the PRD. WAKE_LOCK is deliberately not declared: nothing in the
         app acquires one, and the alert lights the screen through its full-screen intent instead. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".ProjectLumenApplication"
        android:allowBackup="false"
        android:installLocation="internalOnly"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true"
        android:intentMatchingFlags="enforceIntentFilter"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectLumen"
        tools:targetApi="37">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode"
            android:resizeableActivity="true"
            android:theme="@style/Theme.ProjectLumen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <activity
            android:name=".openapi.DashboardActivity"
            android:exported="true"
            android:permission="com.project.lumen.permission.ACCESS_LUMEN_CORE"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode"
            android:resizeableActivity="true"
            android:theme="@style/Theme.ProjectLumen">
            <intent-filter>
                <action android:name="com.project.lumen.action.VIEW_DASHBOARD" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".openapi.RestOverlayActivity"
            android:exported="true"
            android:permission="com.project.lumen.permission.TRIGGER_LUMEN_CONTROL"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode"
            android:resizeableActivity="true"
            android:theme="@style/Theme.Lumen.TransparentOverlay">
            <intent-filter>
                <action android:name="com.project.lumen.action.TRIGGER_REST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".openapi.VisualMonitorActivity"
            android:exported="true"
            android:permission="com.project.lumen.permission.ACCESS_LUMEN_CORE"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode"
            android:resizeableActivity="true"
            android:theme="@style/Theme.ProjectLumen">
            <intent-filter>
                <action android:name="com.project.lumen.action.START_VISUAL_MONITOR" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".core.services.AlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".core.services.ExactAlarmPermissionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".core.services.ReminderActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".core.services.BootReceiver"
            android:exported="false"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".core.proximity.ProximityEventReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.intent.action.CONFIGURATION_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- No intent filter: every alarm and every alert button targets this receiver explicitly,
             so nothing outside the app can drive it and there is no action to filter on. -->
        <receiver
            android:name=".core.services.QuarkKeeperReceiver"
            android:exported="false" />

        <service
            android:name=".core.services.TimerForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps eye-break and Pomodoro timer sessions active while the app is backgrounded." />
        </service>

        <service
            android:name=".core.proximity.ProximityDetectionService"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <service
            android:name=".core.light.LightMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Monitors ambient light and adjusts screen brightness for eye protection." />
        </service>

        <service
            android:name=".core.overlay.EyeProtectionOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Displays a full-screen eye-rest overlay when strict eye protection rules are triggered." />
        </service>

        <service
            android:name=".core.debug.DeveloperDebugOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Shows local-only developer diagnostics for camera, sensors, and foreground service health." />
        </service>

        <!-- The alert surface and the countdown are one service, not two: the same window that has to
             interrupt the user is also what makes the countdown notification non-dismissible, and
             splitting them would leave the countdown unowned between the evening reminder and the
             deadline. -->
        <service
            android:name=".core.services.QuarkKeeperAlertService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Draws the deadline check-in alert on top of the current app and keeps the non-dismissible check-in countdown alive until the user answers it." />
        </service>

        <service
            android:name=".openapi.LumenOpenService"
            android:exported="true"
            android:permission="com.project.lumen.permission.ACCESS_LUMEN_CORE">
            <intent-filter>
                <action android:name="com.project.lumen.action.BIND_OPEN_API" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>
</manifest>
