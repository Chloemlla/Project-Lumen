name: Build Project Lumen VitePress Docs

on:
  push:
    branches:
      - "**"
    paths:
      - ".github/workflows/vitepress-docs.yml"
      - "docs/**"
      # Audit reports live in the repository but are excluded from the published site,
      # so they can never change its output.
      - "!docs/audit/**"
  pull_request:
    branches:
      - "**"
    paths:
      - ".github/workflows/vitepress-docs.yml"
      - "docs/**"
      - "!docs/audit/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: vitepress-docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build VitePress site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Install docs dependencies
        working-directory: docs
        run: npm install

      - name: Build docs
        working-directory: docs
        env:
          VITEPRESS_BASE: /Project-Lumen/
        run: npm run docs:build

      - name: Upload docs artifact
        uses: actions/upload-artifact@v4
        with:
          name: project-lumen-vitepress-docs
          path: docs/.vitepress/dist/**
          retention-days: 14
